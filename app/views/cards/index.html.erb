<h1>MY CARDS</h1>
<div class="my-cards-container">
  <% @cards.each do |card| %>
    <% case card.cat %>
    <% when "weak" %>
      <div class="my-card">
        <h4><%= card.name %></h4>
        <p><%= card.card_type %></p>
        <h5>lvl <%= card.level %></h5>
        <p><%= card.experience %>/<%= card.next_level %> xp</p>
        <h5>prestige <%= card.prestige %></h5>
        <p>power: <%= card.war_power %></p>
        <p>shards <%= card.shards %>/<%= card.next_prestige %></p>
      </div>
    <% when "normal" %>
      <div class="my-card normal-card">
        <h4><%= card.name %></h4>
        <p><%= card.card_type %></p>
        <h5>lvl <%= card.level %></h5>
        <p><%= card.experience %>/<%= card.next_level %> xp</p>
        <h5>prestige <%= card.prestige %></h5>
        <p>power: <%= card.war_power %></p>
        <p>shards <%= card.shards %>/<%= card.next_prestige %></p>
      </div>
    <% when "strong" %>
      <div class="my-card strong-card">
        <h4><%= card.name %></h4>
        <p><%= card.card_type %></p>
        <h5>lvl <%= card.level %></h5>
        <p><%= card.experience %>/<%= card.next_level %> xp</p>
        <h5>prestige <%= card.prestige %></h5>
        <p>power: <%= card.war_power %></p>
        <p>shards <%= card.shards %>/<%= card.next_prestige %></p>
      </div>
    <% when "elite" %>
      <div class="my-card elite-card">
        <h4><%= card.name %></h4>
        <p><%= card.card_type %></p>
        <h5>lvl <%= card.level %></h5>
        <p><%= card.experience %>/<%= card.next_level %> xp</p>
        <h5>prestige <%= card.prestige %></h5>
        <p>power: <%= card.war_power %></p>
        <p>shards <%= card.shards %>/<%= card.next_prestige %></p>
      </div>
    <% end %>
  <% end %>
  <% @cards_locked.each do |card| %>
    <% case card.cat %>
    <% when "weak" %>
      <div class="my-card card-locked">
        <h4><%= card.name %></h4>
        <p><%= card.card_type %></p>
        <p>shards <%= card.shards %>/<%= card.next_prestige %></p>
      </div>
    <% when "normal" %>
      <div class="my-card normal-card card-locked">
        <h4><%= card.name %></h4>
        <p><%= card.card_type %></p>
        <p>shards <%= card.shards %>/<%= card.next_prestige %></p>
      </div>
    <% when "strong" %>
      <div class="my-card strong-card card-locked">
        <h4><%= card.name %></h4>
        <p><%= card.card_type %></p>
        <p>shards <%= card.shards %>/<%= card.next_prestige %></p>
      </div>
    <% when "elite" %>
      <div class="my-card elite-card card-locked">
        <h4><%= card.name %></h4>
        <p><%= card.card_type %></p>
        <p>shards <%= card.shards %>/<%= card.next_prestige %></p>
      </div>
    <% end %>
  <% end %>
</div>
